import 'package:flutter/material.dart';
import 'package:flutter_instagram/models/story_highlights_model.dart';

class StoryHighlightWidget extends StatelessWidget {
  const StoryHighlightWidget({Key? key, required this.model}) : super(key: key);

  final StoryHighlightsModel model;

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
